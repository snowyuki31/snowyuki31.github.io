---
title: モノイド
description: 対象が 1 つの圏。展開すると集合・結合的な積・単位元の組になる。
---

import { Definition, Example } from '@components/env';

<Definition title="モノイド" id="def-monoid">
**モノイド**とは、対象が 1 つの [[圏]] をいう。
</Definition>

同じモノイドは 2 通りに描ける。左は**対象が 1 つ**であることを強調した図で、すべての射が
$\ast$ のループになる。右は**射が複数あって合成される**ことを強調した図で、同じ $\ast$ を
並べて展開したもの。

```tikz
\begin{document}
\begin{tikzpicture}
% 左: 対象が 1 つであること（射は他にもたくさんある）
\node[draw, circle, minimum size=12mm] (M) at (0,0) {$\ast$};
\draw[->,loop above] (M) to node[above] {$1_{\ast}$} (M);
\draw[->,loop left] (M) to node[left] {$m$} (M);
\draw[->,loop right] (M) to node[right] {$m'$} (M);
\draw[->,loop below] (M) to node[below] {$\ldots$} (M);
% 右: 射が複数あって合成されること（同じ ∗ を展開して並べる。鎖はいくらでも続く）
\node (A) at (4.9,0) {$\ast$};
\node (B) at (7.4,0) {$\ast$};
\node (C) at (9.9,0) {$\ast$};
\node (D) at (11.9,0) {$\cdots$};
\draw[->,loop above] (A) to node[above] {$1_{\ast}$} (A);
\draw[->] (A) to node[above] {$m$} (B);
\draw[->] (B) to node[above] {$m'$} (C);
\draw[->] (C) to node[above] {$m''$} (D);
\draw[->] (A) to[bend right=35] node[below] {$m \cdot m'$} (C);
\end{tikzpicture}
\end{document}
```

唯一の対象を $\ast$ とすると、モノイドの実体は射の集まり $M := \mathcal{C}(\ast, \ast)$ と、
その合成 $\cdot$、恒等射 $1_M$ である。これを展開すると、次の代数的な定義が得られる。

<Definition title="モノイド（代数的定義）" id="def-monoid-algebraic">
**モノイド** $(M, \cdot, 1_M)$ とは、集合 $M$、結合的な二項演算
$\cdot \colon M \times M \to M$、および単位元 $1_M \in M$
（任意の $m \in M$ について $1_M \cdot m = m = m \cdot 1_M$）の組をいう。
</Definition>

例: 自然数と加法 $(\mathbb{N}, +, 0)$、文字列と連接 $(\Sigma^{*}, {+\!\!+}, \varepsilon)$、
最大公約数 $(\mathbb{N}, \gcd, 0)$。


<Example title="文字列と連接" id="ex-strings">
アルファベット $\Sigma$ 上の文字列全体 $\Sigma^{*}$ は、連接 ${+\!\!+}$ を積、
空文字列 $\varepsilon$ を単位元とするモノイドになる。

これを対象 1 つの圏として見ると、**文字列が射、連接が合成**である。
たとえば $\mathtt{ab} \colon \ast \to \ast$ と $\mathtt{c} \colon \ast \to \ast$ の合成が
$\mathtt{abc} = \mathtt{ab} \mathbin{+\!\!+} \mathtt{c}$ になる。
</Example>

同じ対象 $\ast$ を並べて描くと、圏の合成の図式がそのまま連接の図になる。
合成を重ねるたびに文字列が伸びていく:
$\mathtt{abc} = \mathtt{ab} \mathbin{+\!\!+} \mathtt{c}$、
$\mathtt{abcba} = \mathtt{abc} \mathbin{+\!\!+} \mathtt{ba}$。

```tikz
\usepackage{tikz-cd}
\begin{document}
\begin{tikzcd}[column sep=large]
\ast \arrow[loop above, "\varepsilon"] \arrow[r, "\mathtt{ab}"] \arrow[rr, bend right=30, "\mathtt{abc}"'] \arrow[rrr, bend right=50, "\mathtt{abcba}"'] & \ast \arrow[r, "\mathtt{c}"] & \ast \arrow[r, "\mathtt{ba}"] & \ast
\end{tikzcd}
\end{document}
```

恒等射は空文字列 $\varepsilon$ で、$\mathtt{ab} \mathbin{+\!\!+} \varepsilon = \mathtt{ab}$ が
単位律にあたる。連接の結合律 $(\mathtt{a} \mathbin{+\!\!+} \mathtt{b}) \mathbin{+\!\!+} \mathtt{c} = \mathtt{a} \mathbin{+\!\!+} (\mathtt{b} \mathbin{+\!\!+} \mathtt{c})$ は、
圏の合成の結合律そのものである。


<Example title="最大公約数" id="ex-gcd">
$(\mathbb{N}, \gcd, 0)$ はモノイドになる。
この場合、自然数は0を含み、$\gcd(n, 0) = n$ が成り立つとする。
</Example>

合成の図式は連接とまったく同じ形だが、今度は積が「潰す」向きに働く。
$12$ と $18$ の合成は $\gcd(12, 18) = 6$、さらに $8$ と合成すると $\gcd(6, 8) = 2$、
$5$ と合成すると $\gcd(2, 5) = 1$（互いに素）まで潰れる。

```tikz
\usepackage{tikz-cd}
\begin{document}
\begin{tikzcd}[column sep=large]
\ast \arrow[loop above, "0"] \arrow[r, "12"] \arrow[rr, bend right=25, "6"'] \arrow[rrr, bend right=40, "2"'] \arrow[rrrr, bend right=55, "1"'] & \ast \arrow[r, "18"] & \ast \arrow[r, "8"] & \ast \arrow[r, "5"] & \ast
\end{tikzcd}
\end{document}
```

